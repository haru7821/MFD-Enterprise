{
  "name": "@mfd/ai-local",
  "version": "0.1.0",
  "private": true,
  "description": "The deterministic layout solver: candidate generation, the two hard gates, and the weighted scoring engine. Pure TypeScript, with the rule engine as its oracle. No model, no network.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@mfd/ai-contract": "workspace:*",
    "@mfd/cad-engine": "workspace:*",
    "@mfd/document-model": "workspace:*",
    "@mfd/layout-knowledge": "workspace:*",
    "@mfd/object-library": "workspace:*",
    "@mfd/rule-engine": "workspace:*"
  }
}
