/**
 * The application version, printed on every report.
 *
 * One constant, because a reader asking "which build produced this?" has to be able to answer
 * it from the page, and a version that lives in three places is a version that disagrees with
 * itself. The top bar and the report both read this.
 */
export const APP_VERSION = '0.5.0';

/** What that version means, for the top bar. Not printed in the report. */
export const APP_STAGE = 'Alpha · Sprint 5 — engineering report';
